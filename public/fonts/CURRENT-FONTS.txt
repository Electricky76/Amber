Typography stack for this site (no extra MyFonts purchases)

SHIPPED IN public/fonts/
  garet-variable.woff2     — Garet (UI / nav). From garet.spacetype.co — confirm license.
  mantonico-extralight.otf — Mantonico Extra Light (headlines). From your MyFonts order.
                           EULA copy: licenses/MyFonts-Monotype-EULA-order-7261873995946.html

LOADED BY NEXT (Google Fonts, free — body + fallback display)
  Cormorant Garamond — fallback if Mantonico fails (CSS variable --font-cormorant).
  Lora               — all body copy (variable --font-lora).

NOT USED (optional later if Amber buys them)
  Pro Condensed Times New Roman — MyFonts
  Ecatherina Bold — MyFonts

Use `font-headline` (Mantonico), `font-label` (Garet), and `font-prose` (Lora) in components — these map to Tailwind v4 `--font-*` theme keys.
